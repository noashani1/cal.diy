{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "NEXTAUTH_SECRET",
      "description": "NextAuth cookie/session signing key. Generate with `openssl rand -base64 32`.",
      "requiredAtBoot": true,
      "generatedForDevelopment": true
    },
    {
      "name": "CALENDSO_ENCRYPTION_KEY",
      "description": "Symmetric AES256 encryption key for stored credentials. Must be exactly 32 bytes (latin1); generate with `openssl rand -base64 24` (yields 32 chars).",
      "requiredAtBoot": true,
      "generatedForDevelopment": true
    }
  ]
}
